<div class="container">

<h2>Party Name</h2>

  <div class="container">
    <!-- Upper container -->
    LINK TO EDIT PARTY IN TOP RIGHT HERE ########
    <div class="party-statistics">
      <div class="party-numbers">
        <h3>Score</h3>
      </div>
      <div class="party-numbers">
        <h3>Members</h3>
      </div>
      <div class="party-leagues">
        <h3>Leagues</h3>
      </div>
    </div>

    <!-- Lower container -->
    <div class="party-lower-container">
      <div class="card">
        <!-- Politician rank, avatar, name and current score. -->
      </div>
    </div>
  </div>

</div>









<!-- #################### Old view ########################## -->

<% if false %>
<div class="homepage-container">
    <h2><%= @political_party.name %></h2>
    <div class="row">
    <% @political_party.politicians.each do |politician| %>
      <div class="card-container col-xs-12 col-sm-6 md-3">
        <div class="card card<%=politician.id%>">

          <div class="front" style="background-image:  linear-gradient(180deg, rgba(0,0,0,0.05) 0%, rgba(0,0,0,0.8) 90%), url(<%= cl_image_path politician.cl_photo %>);">
            <h2 id="first"><%= politician.first_name %></h2>
            <h2 id="last"><%= politician.last_name %></h2>
            <%#= link_to political_party_politician_link_path(@political_party, @political_party.politician_links.find {|pl| pl.politician == politician}), method: :delete do %>
            <!-- <i class="fas fa-minus" id="minus"></i> -->
            <%# end %>
          </div>
          <div class="back">
            <h2>About</h2>
            <p>
              <%= politician.about %>
            </p>
          </div>
        </div>
      </div>
    <% end %>
    </div>
    <% if @political_party.user == current_user %>
      <%= link_to "Add party members", new_political_party_politician_link_path(@political_party) %>
    <% end %>
</div>
<% end %>
